{
  "name": "@mct/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mct": "./bin/mct"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js dist/**/*.test.js",
    "test:e2e": "npm run build && node --test dist/*.e2e.test.js dist/**/*.e2e.test.js"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
