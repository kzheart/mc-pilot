#!/usr/bin/env node
import { syncConfiguredSkills } from "../dist/skill/SkillInstaller.js";
import { buildProgram } from "../dist/index.js";

await syncConfiguredSkills().catch(() => undefined);
buildProgram().parseAsync(process.argv);
